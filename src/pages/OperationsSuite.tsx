import {useEffect,useMemo,useState} from "react";
import {BarChart3,BriefcaseBusiness,CalendarDays,CheckCircle2,Clock3,Download,FileSpreadsheet,Gift,MessageCircle,PauseCircle,PlayCircle,Plus,RefreshCw,ShieldAlert,Tag,Trash2,Users,WalletCards,Wifi} from "lucide-react";
import {ADMIN_EMAILS,BUSINESS_END,BUSINESS_START,desks,Space,spaceLabel,today} from "./types";
import {createAddon,createCoupon,createMembershipPlan,assignMembership,setResourceBlock,removeResourceBlock,loadOperationsSettings,saveOperationsSettings,loadPolicy,savePolicy,loadNotificationTemplates,saveNotificationTemplates,saveShift,savePermissions,toggleAddon,toggleCoupon,setCustomerBlock,updateCustomer,writeOperationLog,updateRefund} from "../lib/firestore";
import "./operations-suite.css";

const money=(n:number)=>`₹${Math.round(Number(n||0)).toLocaleString("en-IN")}`;
const dayValue=(b:any)=>Number(b.days||1);
const isActive=(b:any)=>b.status==="Confirmed"||(b.status==="Pending"&&(b.expiresAt?.toMillis?.()||0)>Date.now());
const csv=(rows:any[][])=>rows.map(r=>r.map(v=>`"${String(v??"").replaceAll('"','""')}"`).join(",")).join("\n");
const download=(name:string,text:string,type="text/csv")=>{const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([text],{type}));a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)};

export default function OperationsSuite({users=[],bookings=[],admins=[],adminLogs=[],actorUid="",actorEmail="",onFlash=()=>{}}:any){
 const [tab,setTab]=useState("overview");
 const [queryText,setQueryText]=useState("");
 const [policy,setPolicy]=useState<any>({maxAdvanceDays:60,businessStart:BUSINESS_START,businessEnd:BUSINESS_END});
 const [ops,setOps]=useState<any>({referralEnabled:true,referralRewardPercent:5,weekendMultiplier:1,peakMultiplier:1});
 const [templates,setTemplates]=useState<any>({});
 const [coupons,setCoupons]=useState<any[]>([]);
 const [plans,setPlans]=useState<any[]>([]);
 const [addons,setAddons]=useState<any[]>([]);
 const [coupon,setCoupon]=useState<any>({code:"",type:"percent",value:10,maxUses:100,active:true});
 const [plan,setPlan]=useState<any>({name:"",description:"",price:5000,days:30,deskDays:20});
 const [addon,setAddon]=useState<any>({name:"",unitPrice:50,space:"all"});
 const [selectedDate,setSelectedDate]=useState(today());
 const [block,setBlock]=useState<any>({inventoryId:"D01",reason:"Maintenance"});
 const [membershipUser,setMembershipUser]=useState("");
 const [customerDraft,setCustomerDraft]=useState<any>({});
 const [permissionDraft,setPermissionDraft]=useState<any>({Manager:{book:true,confirm:true,pricing:false,customers:true,refunds:false},Admin:{book:true,confirm:true,pricing:true,customers:true,refunds:true}});
 const [shift,setShift]=useState<any>({morning:"09:00–15:00",evening:"15:00–19:00",note:""});
 const [refundDraft,setRefundDraft]=useState<any>({});
 useEffect(()=>{loadPolicy().then(setPolicy).catch(()=>{});loadOperationsSettings().then(setOps).catch(()=>{});loadNotificationTemplates().then(setTemplates).catch(()=>{})},[]);
 useEffect(()=>{const un=[] as any[];return()=>{for(const f of un)f?.()};},[]);
 const confirmed=bookings.filter((b:any)=>b.status==="Confirmed");
 const pending=bookings.filter((b:any)=>b.status==="Pending"&&isActive(b));
 const cancelled=bookings.filter((b:any)=>b.status==="Cancelled");
 const revenue=confirmed.reduce((n:number,b:any)=>n+Number(b.total||0),0);
 const todayBookings=bookings.filter((b:any)=>b.date===today()||b.endDate===today());
 const utilization=Math.round((todayBookings.filter((b:any)=>b.status==="Confirmed").reduce((n:number,b:any)=>n+((b.inventoryIds?.length||1)*dayValue(b)),0)/(22*1+3))*100);
 const customerRows=useMemo(()=>{const q=queryText.trim().toLowerCase();return users.filter((u:any)=>!q||[u.name,u.email,u.phone,u.profession,u.company].join(" ").toLowerCase().includes(q));},[users,queryText]);
 const serviceRevenue=useMemo(()=>{const out:any={desk:0,meeting:0,conference:0,podcast:0};confirmed.forEach((b:any)=>out[b.space]=(out[b.space]||0)+Number(b.total||0));return out},[confirmed]);
 const topCustomers=useMemo(()=>{const map=new Map<string,any>();for(const b of confirmed){const email=(b.customerEmail||b.userEmail||"").toLowerCase();const row=map.get(email)||{email,name:b.customerName||email,spend:0,bookings:0};row.spend+=Number(b.total||0);row.bookings+=1;map.set(email,row)}return [...map.values()].sort((a,b)=>b.spend-a.spend).slice(0,6)},[confirmed]);
 const savePolicyAll=async()=>{await savePolicy(policy);await saveOperationsSettings(ops);onFlash("Operating policies saved.")};
 const saveComms=async()=>{await saveNotificationTemplates(templates);onFlash("WhatsApp templates saved.")};
 const addCoupon=async()=>{try{await createCoupon(coupon,actorUid);setCoupon({code:"",type:"percent",value:10,maxUses:100,active:true});onFlash("Coupon saved. Refreshing is live through Firestore.")}catch(e:any){onFlash(e.message||"Could not save coupon")}};
 const addPlan=async()=>{try{await createMembershipPlan(plan,actorUid);setPlan({name:"",description:"",price:5000,days:30,deskDays:20});onFlash("Membership plan created.")}catch(e:any){onFlash(e.message||"Could not create plan")}};
 const addAddon=async()=>{try{await createAddon(addon,actorUid);setAddon({name:"",unitPrice:50,space:"all"});onFlash("Add-on created.")}catch(e:any){onFlash(e.message||"Could not create add-on")}};
 const loadLists=async()=>{onFlash("Live configuration is connected; reload the panel to see newly added catalog items.")};
 const updateCustomerRow=async(u:any)=>{try{await updateCustomer(u.uid,customerDraft[u.uid]||{},actorUid,actorEmail);onFlash(`Customer ${u.email} updated.`)}catch(e:any){onFlash(e.message||"Could not update customer")}};
 const toggleBlock=async(u:any)=>{const blocked=!u.blocked;const reason=customerDraft[u.uid]?.blockedReason||"Repeated policy or payment issue";try{await setCustomerBlock(u.uid,blocked,reason,actorUid,actorEmail);onFlash(blocked?`${u.email} blocked.`:`${u.email} unblocked.`)}catch(e:any){onFlash(e.message||"Could not change customer access")}};
 const exportBookings=()=>download(`coworx-bookings-${today()}.csv`,csv([["Invoice","Customer","Email","Phone","Space","Date","End date","Start","End","Status","Payment","Amount"],...bookings.map((b:any)=>[b.invoiceNumber||"",b.customerName||"",b.customerEmail||b.userEmail||"",b.customerPhone||"",spaceLabel(b.space),b.date,b.endDate||b.date,b.start||"09:00",b.end||"19:00",b.status,b.paymentStatus||b.paymentMethod||"",b.total])]))
 const exportCustomers=()=>download(`coworx-customers-${today()}.csv`,csv([["Name","Email","Phone","Profession","Company","VIP","Blocked","Bookings","Spend"],...users.map((u:any)=>[u.name,u.email,u.phone||"",u.profession||"",u.company||"",u.vip?"Yes":"No",u.blocked?"Yes":"No",u.bookingCount||0,u.totalSpend||0])]))
 const sendBroadcast=()=>{const text=templates.broadcast||"Coworx Central update: new offers and workspace slots are now available.";const recipients=users.filter((u:any)=>u.phone).slice(0,20);for(const u of recipients){const ph=String(u.phone).replace(/\D/g,"");if(ph)window.open(`https://wa.me/${ph.length===10?`91${ph}`:ph}?text=${encodeURIComponent(text.replaceAll("{name}",u.name||"there"))}`,"_blank")};onFlash(`Opened WhatsApp for ${recipients.length} saved contacts. Keep the broadcast batch small to avoid spam.`)};
 const tabs=[['overview','Dashboard'],['customers','Customers'],['revenue','Revenue'],['catalog','Plans & Offers'],['resources','Resources'],['comms','WhatsApp'],['controls','Controls']];
 return <section className="opsSuite">
  <div className="opsHeader"><div><span className="eyebrow">OPERATIONS SUITE</span><h2>Everything in one control room.</h2><p>Live occupancy, customer CRM, pricing policy, memberships, maintenance, communications, permissions and reports.</p></div><div className="opsHeaderActions"><button className="ghost small" onClick={exportBookings}><FileSpreadsheet/> Export bookings</button><button className="ghost small" onClick={exportCustomers}><Users/> Export customers</button></div></div>
  <div className="opsTabs">{tabs.map(([id,label])=><button key={id} className={tab===id?"active":""} onClick={()=>setTab(id)}>{label}</button>)}</div>
  {tab==="overview"&&<>
   <div className="opsMetricGrid"><Metric label="Confirmed revenue" value={money(revenue)} icon={<WalletCards/>}/><Metric label="Today's bookings" value={todayBookings.length} icon={<CalendarDays/>}/><Metric label="Pending holds" value={pending.length} icon={<Clock3/>}/><Metric label="Customers" value={users.length} icon={<Users/>}/><Metric label="Admins" value={admins.length} icon={<ShieldAlert/>}/><Metric label="Utilization signal" value={`${Math.min(100,Math.max(0,utilization))}%`} icon={<BarChart3/>}/></div>
   <div className="opsTwo"><section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">LIVE QUEUE</span><h3>What needs attention</h3></div><button className="ghost small" onClick={()=>setTab("revenue")}><RefreshCw/> Open analytics</button></div>{pending.length?pending.slice(0,5).map((b:any)=><div className="attentionRow" key={b.id}><div><strong>{b.customerName||b.customerEmail}</strong><span>{spaceLabel(b.space)} · {b.date}{b.start?` · ${b.start}–${b.end}`:""}</span></div><b>{money(b.total)}</b></div>):<div className="opsEmpty"><CheckCircle2/><b>No pending holds.</b><span>Everything is ready for the next customer request.</span></div>}</section>
   <section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">SERVICE MIX</span><h3>Revenue by resource</h3></div></div>{Object.entries(serviceRevenue).map(([k,v])=><div className="barRow" key={k}><div><span>{spaceLabel(k as Space)}</span><b>{money(Number(v))}</b></div><div className="bar"><i style={{width:`${revenue?Math.max(3,Number(v)/revenue*100):0}%`}}/></div></div>)}</section></div>
   <section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">TOP CUSTOMERS</span><h3>Frequent and high-value customers</h3></div></div><div className="topCustomerGrid">{topCustomers.map(c=><div className="topCustomer" key={c.email}><span>{c.name.slice(0,1).toUpperCase()}</span><div><b>{c.name}</b><small>{c.bookings} booking{c.bookings===1?"":"s"} · {money(c.spend)}</small></div></div>)}</div></section>
  </>}
  {tab==="customers"&&<section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">CRM</span><h3>Customer database</h3><small>VIP, notes, blacklist, preferences, profile and spend.</small></div><label className="inlineSearch">Search<input value={queryText} onChange={e=>setQueryText(e.target.value)} placeholder="Name, email, phone, profession"/></label></div><div className="crmTable">{customerRows.map((u:any)=><article className={`crmRow ${u.blocked?"blocked":u.vip?"vip":""}`} key={u.uid}><div className="crmIdentity"><span>{(u.name||"?").slice(0,1).toUpperCase()}</span><div><b>{u.name||u.email}</b><small>{u.email} · {u.phone||"No phone"}</small></div></div><div className="crmStats"><span>{u.bookingCount||bookings.filter((b:any)=>lowerEmail(b)===lowerEmail(u)).length} bookings</span><span>{money(u.totalSpend||bookings.filter((b:any)=>lowerEmail(b)===lowerEmail(u)&&b.status==="Confirmed").reduce((n:number,b:any)=>n+Number(b.total||0),0))}</span></div><div className="crmControls"><input value={customerDraft[u.uid]?.profession??u.profession??""} onChange={e=>setCustomerDraft({...customerDraft,[u.uid]:{...(customerDraft[u.uid]||{}),profession:e.target.value}})} placeholder="Profession"/><input value={customerDraft[u.uid]?.company??u.company??""} onChange={e=>setCustomerDraft({...customerDraft,[u.uid]:{...(customerDraft[u.uid]||{}),company:e.target.value}})} placeholder="Company"/><input value={customerDraft[u.uid]?.customerNotes??u.customerNotes??""} onChange={e=>setCustomerDraft({...customerDraft,[u.uid]:{...(customerDraft[u.uid]||{}),customerNotes:e.target.value}})} placeholder="Internal note"/><div className="crmButtons"><button className={`ghost small ${u.vip?"tagOn":""}`} onClick={()=>updateCustomer(u.uid,{vip:!u.vip})}><Tag/> {u.vip?"VIP":"Make VIP"}</button><button className="ghost small" onClick={()=>toggleBlock(u)}>{u.blocked?<PlayCircle/>:<PauseCircle/>} {u.blocked?"Unblock":"Block"}</button><button className="primary small" onClick={()=>updateCustomerRow(u)}>Save profile</button></div></div></article>)}</div></section>}
  {tab==="revenue"&&<><section className="opsMetricGrid"><Metric label="All-time confirmed" value={confirmed.length} icon={<CheckCircle2/>}/><Metric label="Revenue" value={money(revenue)} icon={<WalletCards/>}/><Metric label="Average booking" value={money(confirmed.length?revenue/confirmed.length:0)} icon={<BarChart3/>}/><Metric label="Cancelled" value={cancelled.length} icon={<ShieldAlert/>}/></section><section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">REPORTING</span><h3>Bookings ledger</h3></div><button className="primary small" onClick={exportBookings}><Download/> CSV report</button></div><div className="ledger">{bookings.slice(0,60).map((b:any)=><div className="ledgerRow" key={b.id}><span>{b.invoiceNumber||b.id.slice(0,8)}</span><span>{b.customerName||b.customerEmail||b.userEmail}</span><span>{spaceLabel(b.space)}</span><span>{b.date}{b.endDate&&b.endDate!==b.date?` → ${b.endDate}`:""}</span><span className={`status ${String(b.status).toLowerCase()}`}>{b.status}</span><b>{money(b.total)}</b></div>)}</div></section></>}
  {tab==="catalog"&&<div className="opsTwo opsCatalog"><section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">COUPONS</span><h3>Coupon codes</h3></div><Tag/></div><div className="stackForm"><input value={coupon.code} onChange={e=>setCoupon({...coupon,code:e.target.value.toUpperCase()})} placeholder="WELCOME10"/><select value={coupon.type} onChange={e=>setCoupon({...coupon,type:e.target.value})}><option value="percent">Percent</option><option value="fixed">Fixed ₹</option></select><input type="number" min="1" value={coupon.value} onChange={e=>setCoupon({...coupon,value:Number(e.target.value)})}/><input type="number" min="1" value={coupon.maxUses} onChange={e=>setCoupon({...coupon,maxUses:Number(e.target.value)})}/><button className="primary" onClick={addCoupon}><Plus/> Create coupon</button></div><div className="catalogList">{coupons.map(c=><div key={c.id} className="catalogItem"><b>{c.code}</b><span>{c.value}{c.type==="percent"?"%":"₹"}</span><button className="ghost small" onClick={()=>toggleCoupon(c.id,!c.active,actorUid)}>{c.active?"Pause":"Activate"}</button></div>)}</div></section>
   <section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">MEMBERSHIPS</span><h3>10-day / 20-day / monthly plans</h3></div><BriefcaseBusiness/></div><div className="stackForm"><input value={plan.name} onChange={e=>setPlan({...plan,name:e.target.value})} placeholder="20 Day Flex Pass"/><input value={plan.description} onChange={e=>setPlan({...plan,description:e.target.value})} placeholder="Description"/><input type="number" value={plan.price} onChange={e=>setPlan({...plan,price:Number(e.target.value)})} placeholder="Price"/><input type="number" value={plan.days} onChange={e=>setPlan({...plan,days:Number(e.target.value)})} placeholder="Validity days"/><input type="number" value={plan.deskDays} onChange={e=>setPlan({...plan,deskDays:Number(e.target.value)})} placeholder="Desk days"/><button className="primary" onClick={addPlan}><Plus/> Add membership</button></div><div className="catalogList">{plans.map(p=><div className="catalogItem" key={p.id}><div><b>{p.name}</b><small>{money(p.price)} · {p.deskDays||p.days} desk days</small></div><input className="assignEmail" placeholder="customer email" value={membershipUser} onChange={e=>setMembershipUser(e.target.value)}/><button className="ghost small" onClick={()=>{const u=users.find((x:any)=>lowerEmail(x)===membershipUser.toLowerCase());if(!u)return onFlash("Customer not found in the directory.");assignMembership(u.uid,p,actorUid,actorEmail).then(()=>onFlash("Membership assigned."))}}>Assign</button></div>)}</div></section></div>}
  {tab==="resources"&&<div className="opsTwo"><section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">MAINTENANCE</span><h3>Desk / room maintenance</h3><small>Temporarily disable a resource for a selected date.</small></div><ShieldAlert/></div><div className="stackForm"><input type="date" value={selectedDate} min={today()} onChange={e=>setSelectedDate(e.target.value)}/><select value={block.inventoryId} onChange={e=>setBlock({...block,inventoryId:e.target.value})}>{desks.map(d=><option value={d.id} key={d.id}>{d.id} · Desk {d.number}</option>)}<option value="meeting">Meeting Room</option><option value="conference">Conference Room</option><option value="podcast">Creator Studio</option></select><input value={block.reason} onChange={e=>setBlock({...block,reason:e.target.value})} placeholder="Maintenance reason"/><button className="primary" onClick={()=>setResourceBlock({...block,date:selectedDate,active:true},actorUid).then(()=>onFlash("Resource blocked for maintenance."))}><PauseCircle/> Block resource</button></div><p className="opsNote">Blocked resources should also be disabled in the booking UI when the block is read from Firestore.</p></section><section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">ADD-ONS</span><h3>Printing, lockers, beverages</h3></div><Plus/></div><div className="stackForm"><input value={addon.name} onChange={e=>setAddon({...addon,name:e.target.value})} placeholder="10-page printing"/><input type="number" value={addon.unitPrice} onChange={e=>setAddon({...addon,unitPrice:Number(e.target.value)})} placeholder="Unit price"/><select value={addon.space} onChange={e=>setAddon({...addon,space:e.target.value})}><option value="all">All spaces</option><option value="desk">Desks</option><option value="meeting">Meeting Room</option><option value="conference">Conference Room</option><option value="podcast">Creator Studio</option></select><button className="primary" onClick={addAddon}><Plus/> Add add-on</button></div><div className="catalogList">{addons.map(a=><div className="catalogItem" key={a.id}><b>{a.name}</b><span>{money(a.unitPrice)}</span><button className="ghost small" onClick={()=>toggleAddon(a.id,!a.active,actorUid)}>{a.active?"Pause":"Activate"}</button></div>)}</div></section></div>}
  {tab==="comms"&&<div className="opsTwo"><section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">WHATSAPP TEMPLATES</span><h3>Templates staff can reuse</h3></div><MessageCircle/></div><div className="templateForm">{[["bookingConfirmation","Booking confirmation"],["paymentReminder","Payment reminder"],["cancellation","Cancellation"],["checkIn","Check-in reminder"],["broadcast","Admin broadcast"]].map(([k,l])=><label key={k}>{l}<textarea value={templates[k]||""} onChange={e=>setTemplates({...templates,[k]:e.target.value})} placeholder="Use {name}, {date}, {booking}"/></label>)}</div><button className="primary" onClick={saveComms}><CheckCircle2/> Save templates</button></section><section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">BROADCAST</span><h3>Customer WhatsApp batch</h3><small>Uses saved phone numbers. Opened one-by-one in WhatsApp.</small></div></div><button className="primary" onClick={sendBroadcast}><MessageCircle/> Open WhatsApp batch</button><div className="opsNote">Keep batches small and relevant. WhatsApp may rate-limit rapid messages.</div></section></div>}
  {tab==="controls"&&<><div className="opsTwo"><section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">POLICY</span><h3>Booking limits & pricing rules</h3></div><Clock3/></div><div className="stackForm"><label>Maximum advance days<input type="number" min="1" max="365" value={policy.maxAdvanceDays} onChange={e=>setPolicy({...policy,maxAdvanceDays:Number(e.target.value)})}/></label><label>Business start<input type="time" value={policy.businessStart} onChange={e=>setPolicy({...policy,businessStart:e.target.value})}/></label><label>Business end<input type="time" value={policy.businessEnd} onChange={e=>setPolicy({...policy,businessEnd:e.target.value})}/></label><label>Weekend multiplier<input type="number" min="0.5" step="0.05" value={ops.weekendMultiplier} onChange={e=>setOps({...ops,weekendMultiplier:Number(e.target.value)})}/></label><label>Peak multiplier<input type="number" min="0.5" step="0.05" value={ops.peakMultiplier} onChange={e=>setOps({...ops,peakMultiplier:Number(e.target.value)})}/></label><label><span>Referral program</span><select value={ops.referralEnabled?"on":"off"} onChange={e=>setOps({...ops,referralEnabled:e.target.value==="on"})}><option value="on">Enabled</option><option value="off">Disabled</option></select></label><label>Referral reward %<input type="number" min="0" max="50" value={ops.referralRewardPercent} onChange={e=>setOps({...ops,referralRewardPercent:Number(e.target.value)})}/></label><button className="primary" onClick={savePolicyAll}><CheckCircle2/> Save policies</button></div></section><section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">ROLE PERMISSIONS</span><h3>Manager / Admin capabilities</h3></div><ShieldAlert/></div>{["Manager","Admin"].map(role=><div className="permissionBox" key={role}><b>{role}</b>{Object.keys(permissionDraft[role]).map(k=><label key={k}><input type="checkbox" checked={!!permissionDraft[role][k]} onChange={e=>setPermissionDraft({...permissionDraft,[role]:{...permissionDraft[role],[k]:e.target.checked}})}/>{k}</label>)}</div>)}<button className="primary" onClick={()=>savePermissions(permissionDraft,actorUid).then(()=>onFlash("Role permissions saved."))}>Save permissions</button></section></div><div className="opsTwo"><section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">SHIFTS</span><h3>Manager shifts</h3></div></div><div className="stackForm"><input value={shift.morning} onChange={e=>setShift({...shift,morning:e.target.value})}/><input value={shift.evening} onChange={e=>setShift({...shift,evening:e.target.value})}/><textarea value={shift.note} onChange={e=>setShift({...shift,note:e.target.value})} placeholder="Shift handover note"/><button className="primary" onClick={()=>saveShift(shift,actorUid).then(()=>onFlash("Shift settings saved."))}><BriefcaseBusiness/> Save shifts</button></div></section><section className="opsPanel"><div className="panelHead"><div><span className="eyebrow">REFUNDS</span><h3>Refund workflow</h3></div></div>{bookings.filter((b:any)=>b.refundStatus==="Pending"||b.status==="Cancelled").slice(0,15).map((b:any)=><div className="refundRow" key={b.id}><div><b>{b.customerName||b.customerEmail}</b><small>{b.invoiceNumber||b.id.slice(0,8)} · {money(b.refundAmount||0)}</small></div><input placeholder="Refund reference" value={refundDraft[b.id]||""} onChange={e=>setRefundDraft({...refundDraft,[b.id]:e.target.value})}/><button className="ghost small" onClick={()=>updateRefund(b.id,actorUid,"Processed",b.refundAmount||0,refundDraft[b.id]||"").then(()=>onFlash("Refund marked processed."))}>Mark processed</button></div>)}</section></div></>}
 </section>;
}
function lowerEmail(x:any){return String(x.email||x.customerEmail||x.userEmail||"").toLowerCase()}
function Metric({label,value,icon}:any){return <article className="opsMetric"><span>{icon}</span><small>{label}</small><strong>{value}</strong></article>}
