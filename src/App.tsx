import {useEffect,useMemo,useState} from "react";
import {LogIn,LogOut,ShieldCheck,X} from "lucide-react";
import {GoogleAuthProvider,onAuthStateChanged,signInWithPopup,signOut} from "firebase/auth";
import {doc,getDoc,onSnapshot,serverTimestamp,setDoc,updateDoc} from "firebase/firestore";
import {auth,db,isFirebaseConfigured} from "./firebase";
import {ADMIN_EMAIL,DEFAULT_PRICING,Role,Space,Offer,Booking,emailKey,today,addHours,seats} from "./pages/types";
import {ensureUser,loadPricing,savePricing,assignManager,acceptManager,createOffer,confirmBooking,createBooking,watchUser,watchAllBookings,watchOffers,watchRoleAssignment} from "./lib/firestore";
import Home from "./pages/Home";import BookingPage from "./pages/Booking";import Amenities from "./pages/Amenities";import {Account,OffersPage,ManagerPage,AdminPage,LoginPage} from "./pages/Other";
export default function App(){
 const [user,setUser]=useState<any>(null),[role,setRole]=useState<Role>("User"),[page,setPage]=useState("home"),[theme,setTheme]=useState<"dark"|"light">((localStorage.getItem("coworx-theme") as any)||"dark");
 const [date,setDate]=useState(today()),[space,setSpace]=useState<Space>("cubicle"),[seat,setSeat]=useState<string|null>(null),[conf,setConf]=useState("09:00"),[pod,setPod]=useState("10:00"),[prices,setPrices]=useState(DEFAULT_PRICING);
 const [bookings,setBookings]=useState<Booking[]>([]),[allBookings,setAllBookings]=useState<Booking[]>([]),[offers,setOffers]=useState<Offer[]>([]),[assignment,setAssignment]=useState<any>(null),[flash,setFlash]=useState(""),[loginError,setLoginError]=useState("");
 const [managerEmail,setManagerEmail]=useState(""),[offerForm,setOfferForm]=useState({title:"",description:"",type:"percent" as any,value:10,targetType:"all" as any,targetEmail:""});
 useEffect(()=>{document.documentElement.dataset.theme=theme;localStorage.setItem("coworx-theme",theme)},[theme]);
 useEffect(()=>onAuthStateChanged(auth,async u=>{setUser(u);if(!u){setRole("User");return}try{setRole(await ensureUser(u))}catch(e:any){setFlash(e?.message||"Could not load account")}}),[]);
 useEffect(()=>{loadPricing().then(setPrices)},[]);
 useEffect(()=>{if(!user){setBookings([]);setOffers([]);setAssignment(null);return}const a=watchUser(user.uid,setBookings),b=watchOffers(user.email||"",setOffers),c=watchRoleAssignment(user.email||"",setAssignment);return()=>{a();b();c()}},[user]);
 useEffect(()=>{if(role==="User"){setAllBookings([]);return}return watchAllBookings(setAllBookings)},[role]);
 const booked=useMemo(()=>{const ids=allBookings.filter(b=>b.date===date&&(b.status==="Confirmed"||(b.status==="Pending"&&(b.expiresAt?.toMillis?.()||0)>Date.now()))).map(b=>b.inventoryId);return ids.length?ids:["seat-A2","seat-C3","seat-D1"]},[allBookings,date]);
 const selected=seats.find(s=>s.id===seat),base=space==="cubicle"?(selected?.premium?prices.cubicle_premium:prices.cubicle_basic):space==="conference"?prices.conference_slot:prices.podcast_hourly;
 const off=offers[0],discount=off?(off.type==="percent"?Math.round(base*off.value/100):Math.min(base,off.value)):0,total=Math.max(0,base-discount);
 const nav=(p:string)=>{if(p==="book"&&!user){setPage("login");setFlash("Google sign-in is required before booking.");return}setPage(p)};
 async function login(){if(!isFirebaseConfigured){setLoginError("Edit src/firebaseConfig.ts with your Firebase Web App values.");return}try{await signInWithPopup(auth,new GoogleAuthProvider());setPage("home");setLoginError("")}catch(e:any){setLoginError(e?.message||"Google login failed")}}
 async function book(){if(!user){nav("book");return}if(space==="cubicle"&&!seat)return setFlash("Select a seat first.");const start=space==="conference"?conf:space==="podcast"?pod:undefined,end=start?addHours(start,space==="conference"?5:1):undefined,iid=space==="cubicle"?`seat-${seat}`:space,label=space==="cubicle"?`${selected?.premium?"Premium":"Standard"} seat ${seat}`:space==="conference"?"Conference Room":"Podcast Studio";try{await createBooking({date,space,inventoryId:iid,label,userId:user.uid,userEmail:user.email,start,end,base,discount,total,offerId:off?.id||null});window.open(`https://wa.me/${"919970836509"}?text=${encodeURIComponent(`Need ${label}, ${date}.${start?` Time: ${start}-${end}.`:""} Total Price: ₹${total}. Offer: ${off?.title||"None"}. What will be the best offer?`)}`,"_blank");setFlash("Request created. Selection held for 15 minutes.");setPage("bookings")}catch(e:any){setFlash(e?.message||"Booking failed")}}
 async function doAssign(){try{await assignManager(managerEmail,user.uid);setManagerEmail("");setFlash("Manager invitation sent.")}catch(e:any){setFlash(e?.message||"Unable to assign manager")}}
 async function doOffer(){try{await createOffer(offerForm,user.uid);setOfferForm({title:"",description:"",type:"percent",value:10,targetType:"all",targetEmail:""});setFlash("Offer created.")}catch(e:any){setFlash(e?.message||"Offer creation failed")}}
 async function doConfirm(id:string){try{await confirmBooking(id,user.uid);setFlash("Booking confirmed.")}catch(e:any){setFlash(e?.message||"Could not confirm booking")}}
 async function accept(){try{await acceptManager(assignment,user.uid);setRole("Manager");setAssignment({...assignment,status:"accepted"});setFlash("Manager role activated.")}catch(e:any){setFlash(e?.message||"Could not activate role")}}
 return <div className="app"><header><button className="brand" onClick={()=>setPage("home")}><span className="brandMark">C</span><span><b>coworx</b> central<small>SOLAPUR CITY</small></span></button><nav><button onClick={()=>{setSpace("cubicle");setSeat(null);nav("book")}}>Book</button>{user&&<><button onClick={()=>setPage("bookings")}>My Bookings</button><button onClick={()=>setPage("offers")}>Offers{offers.length?` (${offers.length})`:""}</button></>}<button onClick={()=>setPage("amenities")}>Amenities</button>{role!=="User"&&user&&<button onClick={()=>setPage("manager")}>Manager</button>}{role==="Admin"&&user&&<button onClick={()=>setPage("admin")}>Admin</button>}</nav><div className="headActions">{user?<><span className="emailChip">{user.email}</span><button className="logout" onClick={()=>signOut(auth)}><LogOut size={16}/></button></>:<button className="loginChip" onClick={login}><LogIn size={16}/> Google</button>}<button className="theme" onClick={()=>setTheme(theme==="dark"?"light":"dark")}>{theme==="dark"?"☀":"☾"}</button></div></header>
 {assignment?.status==="pending"&&<div className="roleBanner"><div><ShieldCheck/><span><b>Manager invitation</b><small>Admin has invited {user.email}.</small></span></div><div><button className="primary small" onClick={accept}>Accept</button><button className="ghost small" onClick={()=>updateDoc(doc(db,"roleAssignments",assignment.id),{status:"declined"})}>Decline</button></div></div>}
 {!isFirebaseConfigured&&<div className="configBanner">Edit <code>src/firebaseConfig.ts</code> with your Firebase Web App values.</div>}{flash&&<div className="toast">{flash}<button onClick={()=>setFlash("")}><X size={15}/></button></div>}
 {page==="home"&&<Home book={s=>{setSpace(s);setSeat(null);nav("book")}} prices={prices}/>} {page==="login"&&<LoginPage onLogin={login} error={loginError}/>} {page==="book"&&user&&<BookingPage date={date} setDate={setDate} space={space} setSpace={setSpace} seat={seat} setSeat={setSeat} booked={booked} prices={prices} conf={conf} setConf={setConf} pod={pod} setPod={setPod} base={base} total={total} discount={discount} offers={offers} book={book}/>} {page==="bookings"&&user&&<Account bookings={bookings}/>} {page==="offers"&&user&&<OffersPage offers={offers}/>} {page==="amenities"&&<Amenities/>} {page==="manager"&&role!=="User"&&<ManagerPage bookings={allBookings} form={offerForm} setForm={setOfferForm} createOffer={doOffer} confirm={doConfirm}/>} {page==="admin"&&role==="Admin"&&<AdminPage prices={prices} setPrices={setPrices} save={()=>savePricing(prices).then(()=>setFlash("Pricing saved."))} email={managerEmail} setEmail={setManagerEmail} assign={doAssign}/>}<footer><b>coworx central</b><span>Solapur City</span><a href="https://share.google/jMdqj9zrUAniaTc5b" target="_blank" rel="noreferrer">Google Maps</a></footer></div>
}
